
// painter's algorithm doesn't quite must pick either front or back for the secant plane
// if the plane was cut into multiple parts drawing could be better...
// but freeze this idea --- transparent secant planes look good enough "rgba(200,0,0,0.1)"

// given a ser of vertices on the 3D solid and the corners
// of the secand plane, cuts the secant plane into pieces
// that wrap around the object
var computePlanePieces = function(vertices,corners,idx_start) {
    
    var closest = function (v,corners){
        
        
    }
    
    // build vertices and corner structures
    
    
    return { new_vertices: new_vertices,
             new_faces: new_faces };
};


